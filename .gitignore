.env
target/
*.db

# Local-only Claude Code project guide (kept out of version control by request).
CLAUDE.md

# Local-only scratch / audit notes (not for the public repo).
plan-*.md

# Local design references (redesign brief + HTML prototypes) — not for the public repo.
Design/
web-redesign-brief.md

# macOS junk
.DS_Store

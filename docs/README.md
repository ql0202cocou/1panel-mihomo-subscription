# 文档

已实现服务(后端 + SPA)的维护阶段参考文档。

- `api-design.md`:API 请求/响应契约、认证、转换器顶层键处理。
- `data-model.md`:SQLite 模式、索引、迁移。
- `security-design.md`:安全目标、公共链接、SSRF、认证、滥用控制。
- `1panel-app.md`:1Panel 上用 docker compose 部署 + 权威环境变量表(已弃用应用包)。
- `release.md`:镜像构建、打标签、发布步骤。
- `changelog.md`:变更日志(倒序,保留全部历史)。
